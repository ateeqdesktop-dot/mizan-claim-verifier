# تدقيق الفجوات — Mizan

## الخلاصة التنفيذية

المشروع الحالي ليس نموذجًا شكليًا؛ فهو يحتوي على خط بيانات حقيقي، نموذج تصنيف متعدد الفئات، مسترجع أدلة، خدمة امتناع، واجهة FastAPI، اختبارات، رسوم تقييم، وتوثيق أولي. لذلك لا تحتاج النسخة الجديدة إلى تغيير الفكرة الأساسية. القرار الأفضل وفق أولوية **القيمة الواقعية ثم الجودة التقنية ثم الاعتمادية** هو ترقية ميزان إلى نسخة Portfolio قابلة للعرض، بدل هدم نظام يعمل وإعادة بناء مشروع مختلف أقل نضجًا.

## نقاط القوة الموجودة

| المجال | الموجود فعليًا | دليل المراجعة |
|---|---|---|
| الفكرة | تحقق عربي قائم على الأدلة بدل تصنيف أخبار ثنائي | README وقرارات المقارنة |
| البيانات | AraFacts حقيقية مع توثيق المصدر والترخيص والتوزيع | `README.md` و`project_research_notes.md` |
| AI/ML | TF-IDF للكلمات والحروف مع Logistic Regression، واسترجاع TF-IDF مستقل | `src/mizan/model.py` و`src/mizan/retriever.py` |
| التقييم | تقسيم زمني وطبقي، claim-only، oracle evidence، retrieved evidence | `scripts/train.py` و`artifacts/metrics.json` |
| السلامة | `insufficient_evidence` عند ضعف الدليل أو الثقة | `src/mizan/service.py` |
| التشغيل | CLI وFastAPI وDocker وMakefile | `scripts/predict.py` و`src/mizan/api.py` |
| الاختبارات | 9 اختبارات وحدة وتكامل ناجحة | `tests/` ونتيجة `pytest` وCI |
| التوثيق | README وتصميم ومعمارية وتقييم ومراجع | `README.md` و`docs/` |

## الفجوات ذات الأولوية

| الأولوية | الفجوة | أثرها | المعالجة المعتمدة |
|---:|---|---|---|
| 1 | كانت لا توجد واجهة Demo مرئية | عولجت بإضافة طبقة عرض مناسبة للـPortfolio | أضيفت واجهة HTML/CSS/JavaScript عربية RTL تخدمها FastAPI وتعرض الحكم والدليل والامتناع والمرشحين، واختُبرت بصريًا وفعليًا عبر HTTP |
| 2 | كانت لا توجد CI | عولجت بإضافة تحقق تلقائي عند كل تغيير | GitHub Actions تشغّل repository hygiene واختبارات Python 3.10–3.12 وcompileall وgit diff --check، وآخر تشغيل ناجح |
| 3 | كان سجل Git الأولي commit واحدًا | عولجت قابلية التتبع دون إعادة كتابة التاريخ المنشور | أصبح السجل متعدد commits منطقية تشمل التأسيس، التنفيذ، Demo/benchmark/CI، التوثيق، الترخيص، والتقرير النهائي |
| 4 | كانت اختبارات الحالات السلبية محدودة | عولجت بإضافة تغطية لحالات الامتناع والتحقق والأخطاء | تغطي الاختبارات validation وmissing artifacts وabstention وmetadata وAPI errors |
| 5 | كانت لا توجد مقارنة baseline واضحة في التنفيذ | عولجت بإضافة benchmark قابل لإعادة التشغيل | يقارن `Majority` و`LinearSVC` و`Logistic Regression` على التقسيمين الزمني والطبقي مع حفظ النتائج الفعلية |
| 6 | كانت API تستخدم `on_event` القديم | عولجت بإزالة التحذير المتوقع | انتقلت دورة حياة FastAPI إلى `lifespan` handler مع الحفاظ على السلوك الحالي |
| 7 | كانت لا توجد صفحة Model Card أو تقرير قرار | عولجت بإضافة وثائق مراجعة مستقلة | أضيفت `docs/model_card.md` و`docs/project_decision.md` و`docs/final_report.md` مع الفئات النادرة وسياسة الامتناع |
| 8 | لا يوجد ملف بيئة/إعدادات واضح للواجهة | ليست فجوة لازمة في النسخة المحلية الحالية | الواجهة لا تحتاج أسرارًا أو متغيرات بيئية؛ الإعدادات الآمنة الافتراضية موثقة، ويمكن إضافة إعدادات نشر لاحقًا إذا توسع النظام |
| 9 | كان لا يوجد فحص أمني تلقائي | عولجت الفجوة الأساسية دون إدخال اعتماد إضافي | CI يتضمن فحص أنماط الأسرار والملفات غير المرغوبة؛ أما `pip-audit` فيبقى تحسينًا اختياريًا لاحقًا لتجنب كسر قابلية التشغيل دون اتصال إضافي |

## قرار النطاق

لن نضيف قاعدة بيانات أو مصادقة أو اتصالًا حيًا بمصادر ويب في هذه النسخة؛ لا تحتاج مهمة التحقق القابلة لإعادة الإنتاج هذه المكونات، وإضافتها ستزيد سطح الهجوم والتعقيد دون قيمة تناسب الهدف. ستكون الواجهة المحلية طبقة عرض رقيقة فوق API الحالي، بينما يبقى النموذج وخط التقييم مستقلين وقابلين للاختبار.

## معايير القبول للنسخة المحسنة

1. يمكن تشغيل Demo محليًا بأمر واحد بعد وجود artifacts، ويعرض حالات النجاح والامتناع والخطأ بصورة مفهومة.
2. تستمر اختبارات النموذج وAPI، وتضاف حالات حواف بحيث لا يقل عدد الاختبارات عن النسخة الحالية.
3. ينجح GitHub Actions في بيئة نظيفة دون بيانات خام أو أسرار.
4. تتضمن وثائق المشروع مقارنة baseline وModel Card وقيود البيانات والترخيص.
5. يبقى القرار النهائي محافظًا: لا يعرض `verdict` قطعيًا عند غياب دليل كافٍ.
6. لا تُرفع البيانات الخام أو مفاتيح API أو ملفات النماذج الكبيرة إلى GitHub.

## التغييرات المؤجلة عمدًا

الاسترجاع الدلالي العربي وreranker وstance classifier تحسينات بحثية قيّمة، لكنها تحتاج بيانات أزواج evidence/stance موثوقة وضبطًا مستقلًا. لن ندخلها في النسخة المحسنة كادعاء غير مثبت؛ ستبقى ضمن خطة التطوير المستقبلية مع إبقاء baseline الحالي شفافًا وقابلًا لإعادة الإنتاج.
